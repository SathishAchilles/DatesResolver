source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in dates_resolver.gemspec
gemspec

group :development, :test do
  gem 'reek', '~> 6.0', '>= 6.0.3'
  gem 'rubocop', '~> 1.8', '>= 1.8.1'
end
